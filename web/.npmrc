# Inngest declares @sveltejs/kit as an optional peer dep, which transitively
# pulls vite 6 and conflicts with our vite 7 (via vitest 4). The optional
# peer is genuinely unused — we don't import from inngest/sveltekit. The
# legacy resolver respects optional-peer status and skips the install.
legacy-peer-deps=true
